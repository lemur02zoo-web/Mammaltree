[build]
  publish = "public"

[functions]
  node_bundler = "esbuild"

[[headers]]
  for = "/mdd_full.json"
  [headers.values]
    Cache-Control = "public, max-age=86400"

# SPA fallback — must be last
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
